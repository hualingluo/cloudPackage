name: 🎬 抖剧 Douju - Flutter EXE 自动化打包

on:
  push:
    branches:
      - master
  workflow_dispatch: {}

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: 1. 检出仓库代码
        uses: actions/checkout@v4

      - name: 2. 安装 Flutter 环境
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 3. 初始化 Flutter 项目
        run: |
          flutter create --platforms=windows player_app
          cd player_app
          flutter config --enable-windows-desktop

      - name: 4. 注入智能播放器引擎与数据
        shell: pwsh
        run: |
          # 确保 assets 目录存在
          $assetsPath = "player_app/assets"
          if (!(Test-Path $assetsPath)) { New-Item -ItemType Directory -Path $assetsPath }
          
          # 优先使用工程导出的 project.json，否则回退到 metadata.json
          if (Test-Path "project.json") {
              Copy-Item "project.json" -Destination "player_app/assets/movie_config.json" -Force
          } else {
              Copy-Item "metadata.json" -Destination "player_app/assets/movie_config.json" -Force
          }
          
          # 写入具有协议自愈能力的 Dart 源码
          $dartCode = @'
          import 'dart:convert';
          import 'dart:async';
          import 'package:flutter/material.dart';
          import 'package:flutter/services.dart';
          import 'package:video_player/video_player.dart';

          void main() {
            WidgetsFlutterBinding.ensureInitialized();
            runApp(const MovieApp());
          }

          class MovieApp extends StatelessWidget {
            const MovieApp({super.key});
            @override
            Widget build(BuildContext context) {
              return MaterialApp(
                title: 'Douju Player Pro',
                debugShowCheckedModeBanner: false,
                theme: ThemeData(
                  brightness: Brightness.dark,
                  primaryColor: Colors.cyan,
                  useMaterial3: true,
                  scaffoldBackgroundColor: Colors.black,
                ),
                home: const MoviePlayer(),
              );
            }
          }

          class MoviePlayer extends StatefulWidget {
            const MoviePlayer({super.key});
            @override
            State<MoviePlayer> createState() => _MoviePlayerState();
          }

          class _MoviePlayerState extends State<MoviePlayer> {
            Map<String, dynamic>? project;
            String? currentId;
            VideoPlayerController? _videoController;
            bool _isInitialized = false;
            String? _errorMessage;
            bool _isLoading = true;

            @override
            void initState() {
              super.initState();
              _loadProject();
            }

            @override
            void dispose() {
              _videoController?.dispose();
              super.dispose();
            }

            Future<void> _loadProject() async {
              try {
                final String response = await rootBundle.loadString('assets/movie_config.json');
                final data = await json.decode(response);
                setState(() {
                  project = data;
                  currentId = 'start';
                });
                _setupMedia();
              } catch (e) {
                setState(() { _errorMessage = "配置文件读取错误: $e"; _isLoading = false; });
              }
            }

            Future<void> _setupMedia() async {
              final nodes = project?['nodes'] as Map<String, dynamic>?;
              final node = nodes?[currentId];
              if (node == null) return;

              setState(() { 
                _isInitialized = false; 
                _isLoading = true; 
                _errorMessage = null; 
              });

              await _videoController?.dispose();
              _videoController = null;

              String mediaSrc = (node['mediaSrc'] ?? '').toString();
              final String mediaType = (node['mediaType'] ?? 'none').toString();
              
              bool isVideo = (mediaType == 'video') || (mediaSrc.toLowerCase().contains('.mp4'));

              if (isVideo && mediaSrc.isNotEmpty) {
                // 尝试首选协议加载
                bool success = await _tryInitVideo(mediaSrc);
                
                // 如果是 http 且失败了，尝试自动升级到 https (解决 Windows 混合内容安全策略问题)
                if (!success && mediaSrc.startsWith('http://')) {
                  String secureSrc = mediaSrc.replaceFirst('http://', 'https://');
                  debugPrint("HTTP加载失败，尝试 HTTPS: $secureSrc");
                  success = await _tryInitVideo(secureSrc);
                }

                if (!success && mounted) {
                  setState(() {
                    _errorMessage = "视频加载失败：资源无法访问或格式不受支持。\n地址: $mediaSrc";
                    _isLoading = false;
                  });
                }
              } else {
                setState(() { _isLoading = false; });
              }
            }

            Future<bool> _tryInitVideo(String url) async {
              try {
                _videoController = VideoPlayerController.networkUrl(Uri.parse(url));
                // Windows 平台建议稍微延长超时
                await _videoController!.initialize().timeout(const Duration(seconds: 20));
                await _videoController!.setLooping(true);
                await _videoController!.setVolume(1.0);
                await _videoController!.play();
                
                if (mounted) {
                  setState(() { _isInitialized = true; _isLoading = false; });
                }
                return true;
              } catch (e) {
                debugPrint("Video Init Try Failed ($url): $e");
                await _videoController?.dispose();
                _videoController = null;
                return false;
              }
            }

            void _onOptionPressed(String targetId) {
              if (targetId.isEmpty) return;
              setState(() { currentId = targetId; });
              _setupMedia();
            }

            @override
            Widget build(BuildContext context) {
              if (project == null) return const Scaffold(body: Center(child: CircularProgressIndicator(color: Colors.cyan)));
              
              final nodes = project!['nodes'] as Map<String, dynamic>?;
              final node = nodes?[currentId];
              
              if (node == null) return const Scaffold(body: Center(child: Text('剧终', style: TextStyle(fontSize: 32))));

              return Scaffold(
                body: Stack(
                  children: [
                    Positioned.fill(child: _buildMediaBackground(node)),
                    Positioned.fill(
                      child: Container(
                        decoration: BoxDecoration(
                          gradient: LinearGradient(
                            begin: Alignment.topCenter,
                            end: Alignment.bottomCenter,
                            colors: [Colors.transparent, Colors.black.withOpacity(0.85)],
                          ),
                        ),
                      ),
                    ),
                    if (_isLoading)
                      const Center(child: CircularProgressIndicator(color: Colors.cyanAccent)),
                    Align(
                      alignment: Alignment.bottomCenter,
                      child: Container(
                        padding: const EdgeInsets.symmetric(horizontal: 60, vertical: 80),
                        child: Column(
                          mainAxisSize: MainAxisSize.min,
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            Text(
                              node['title'] ?? '', 
                              style: const TextStyle(
                                fontSize: 48, 
                                fontWeight: FontWeight.bold, 
                                color: Colors.cyanAccent, 
                                shadows: [Shadow(blurRadius: 15, color: Colors.black)]
                              )
                            ),
                            const SizedBox(height: 24),
                            Text(
                              node['content'] ?? '', 
                              style: const TextStyle(
                                fontSize: 22, 
                                height: 1.6, 
                                color: Colors.white, 
                                shadows: [Shadow(blurRadius: 8, color: Colors.black)]
                              )
                            ),
                            const SizedBox(height: 50),
                            Wrap(
                              spacing: 20,
                              runSpacing: 20,
                              children: (node['options'] as List? ?? []).map((opt) => ElevatedButton(
                                style: ElevatedButton.styleFrom(
                                  padding: const EdgeInsets.symmetric(horizontal: 48, vertical: 28),
                                  backgroundColor: Colors.white.withOpacity(0.08),
                                  foregroundColor: Colors.white,
                                  side: const BorderSide(color: Colors.cyanAccent, width: 1.5),
                                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)),
                                  elevation: 0,
                                ),
                                onPressed: () => _onOptionPressed(opt['targetId']?.toString() ?? ''),
                                child: Text(opt['label']?.toString() ?? '继续', style: const TextStyle(fontSize: 20)),
                              )).toList(),
                            ),
                          ],
                        ),
                      ),
                    ),
                    if (_errorMessage != null)
                      Positioned(
                        top: 40,
                        left: 40,
                        right: 40,
                        child: Container(
                          padding: const EdgeInsets.all(16),
                          decoration: BoxDecoration(
                            color: Colors.red.withOpacity(0.8),
                            borderRadius: BorderRadius.circular(8),
                            border: Border.all(color: Colors.white30),
                          ),
                          child: Column(
                            children: [
                              const Icon(Icons.error_outline, color: Colors.white, size: 28),
                              const SizedBox(height: 8),
                              Text(_errorMessage!, textAlign: TextAlign.center, style: const TextStyle(color: Colors.white, fontSize: 14)),
                            ],
                          ),
                        ),
                      ),
                  ],
                ),
              );
            }

            Widget _buildMediaBackground(Map<String, dynamic> node) {
              final mediaSrc = node['mediaSrc']?.toString() ?? '';
              
              if (_isInitialized && _videoController != null) {
                return SizedBox.expand(
                  child: FittedBox(
                    fit: BoxFit.cover,
                    child: SizedBox(
                      width: _videoController!.value.size.width,
                      height: _videoController!.value.size.height,
                      child: VideoPlayer(_videoController!),
                    ),
                  ),
                );
              } else if (mediaSrc.isNotEmpty && !mediaSrc.toLowerCase().contains('.mp4')) {
                return Image.network(
                  mediaSrc, 
                  fit: BoxFit.cover, 
                  errorBuilder: (c, e, s) => Container(color: Colors.black)
                );
              }
              return Container(color: Colors.black);
            }
          }
          '@
          
          # 缩进处理并写入文件
          $dartCode.Trim() -replace '(?m)^\s{10}', '' | Out-File -FilePath "player_app/lib/main.dart" -Encoding utf8

      - name: 5. 注册资源与依赖
        shell: pwsh
        run: |
          cd player_app
          flutter pub add video_player
          $content = Get-Content pubspec.yaml
          $newContent = $content -replace '# assets:', "assets:`n    - assets/movie_config.json"
          Set-Content pubspec.yaml $newContent

      - name: 6. 编译 Release EXE
        run: |
          cd player_app
          flutter pub get
          flutter build windows --release

      - name: 7. 整理并打包产物
        shell: pwsh
        run: |
          if (Test-Path "output") { Remove-Item -Recurse -Force "output" }
          New-Item -ItemType Directory -Path "output"
          Copy-Item "player_app/build/windows/x64/runner/Release/*" -Destination "output/" -Recurse
          Compress-Archive -Path "output/*" -DestinationPath "Douju_Player_Windows.zip" -Force

      - name: 8. 上传 Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Douju-Flutter-EXE-Build
          path: Douju_Player_Windows.zip
          retention-days: 7
