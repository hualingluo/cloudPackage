
name: 🎬 抖剧 Douju - Flutter EXE 自动化打包

on:
  push:
    branches:
      - master
  workflow_dispatch: {}

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: 1. 检出仓库代码
        uses: actions/checkout@v4

      - name: 2. 安装 Flutter 环境
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 3. 初始化 Flutter 项目
        run: |
          flutter create --platforms=windows player_app
          cd player_app
          flutter config --enable-windows-desktop

      - name: 4. 注入智能播放器引擎与数据
        shell: pwsh
        run: |
          $assetsPath = "player_app/assets"
          if (!(Test-Path $assetsPath)) { New-Item -ItemType Directory -Path $assetsPath }
          
          if (Test-Path "project.json") {
              Copy-Item "project.json" -Destination "player_app/assets/movie_config.json" -Force
          } else {
              Copy-Item "metadata.json" -Destination "player_app/assets/movie_config.json" -Force
          }
          
          # 写入具有协议自适应和错误自愈能力的播放器
          $dartCode = @'
          import 'dart:convert';
          import 'dart:async';
          import 'dart:io';
          import 'package:flutter/material.dart';
          import 'package:flutter/services.dart';
          import 'package:video_player/video_player.dart';

          void main() {
            HttpOverrides.global = MyHttpOverrides(); // 强制允许所有证书和混合内容
            WidgetsFlutterBinding.ensureInitialized();
            runApp(const MovieApp());
          }

          class MyHttpOverrides extends HttpOverrides {
            @override
            HttpClient createHttpClient(SecurityContext? context) {
              return super.createHttpClient(context)
                ..badCertificateCallback = (X509Certificate cert, String host, int port) => true;
            }
          }

          class MovieApp extends StatelessWidget {
            const MovieApp({super.key});
            @override
            Widget build(BuildContext context) {
              return MaterialApp(
                title: 'Douju Player Pro',
                debugShowCheckedModeBanner: false,
                theme: ThemeData(
                  brightness: Brightness.dark,
                  primaryColor: Colors.cyan,
                  useMaterial3: true,
                  scaffoldBackgroundColor: Colors.black,
                ),
                home: const MoviePlayer(),
              );
            }
          }

          class MoviePlayer extends StatefulWidget {
            const MoviePlayer({super.key});
            @override
            State<MoviePlayer> createState() => _MoviePlayerState();
          }

          class _MoviePlayerState extends State<MoviePlayer> {
            Map<String, dynamic>? project;
            String? currentId;
            VideoPlayerController? _videoController;
            bool _isInitialized = false;
            String? _errorMessage;
            String _loadingStatus = "初始化中...";
            bool _isLoading = true;
            Timer? _timeoutTimer;

            @override
            void initState() {
              super.initState();
              _loadProject();
            }

            @override
            void dispose() {
              _timeoutTimer?.cancel();
              _videoController?.dispose();
              super.dispose();
            }

            Future<void> _loadProject() async {
              try {
                final String response = await rootBundle.loadString('assets/movie_config.json');
                final data = await json.decode(response);
                setState(() {
                  project = data;
                  currentId = 'start';
                });
                _setupMedia();
              } catch (e) {
                setState(() { _errorMessage = "配置文件加载错误"; _isLoading = false; });
              }
            }

            Future<void> _setupMedia() async {
              _timeoutTimer?.cancel();
              final nodes = project?['nodes'] as Map<String, dynamic>?;
              final node = nodes?[currentId];
              if (node == null) return;

              setState(() { 
                _isInitialized = false; 
                _isLoading = true; 
                _errorMessage = null; 
                _loadingStatus = "正在建立资源连接...";
              });

              await _videoController?.dispose();
              _videoController = null;

              String mediaSrc = (node['mediaSrc'] ?? '').toString();
              bool isVideo = mediaSrc.toLowerCase().contains('.mp4') || (node['mediaType'] == 'video');

              if (isVideo && mediaSrc.isNotEmpty) {
                // 开启 15 秒强制超时监控，如果卡死则转入降级模式
                _timeoutTimer = Timer(const Duration(seconds: 15), () {
                  if (_isLoading && mounted) {
                    setState(() {
                      _isLoading = false;
                      _errorMessage = "视频连接超时，已自动进入剧本模式。\nURL: $mediaSrc";
                    });
                  }
                });

                bool success = await _tryInitVideo(mediaSrc, "正在解析媒体流...");
                
                if (!success && mediaSrc.startsWith('http://')) {
                  String secureSrc = mediaSrc.replaceFirst('http://', 'https://');
                  success = await _tryInitVideo(secureSrc, "正在尝试加密通道...");
                }

                if (!success && mounted) {
                  setState(() {
                    _isLoading = false;
                    _errorMessage = "无法播放该视频，您可以继续阅读剧本。";
                  });
                }
              } else {
                setState(() { _isLoading = false; });
              }
            }

            Future<bool> _tryInitVideo(String url, String statusMsg) async {
              try {
                if (mounted) setState(() => _loadingStatus = statusMsg);
                _videoController = VideoPlayerController.networkUrl(
                  Uri.parse(url),
                  videoPlayerOptions: VideoPlayerOptions(allowBackgroundPlayback: false),
                );
                
                await _videoController!.initialize().timeout(const Duration(seconds: 10));
                
                if (mounted) {
                  await _videoController!.setLooping(true);
                  await _videoController!.setVolume(1.0);
                  await _videoController!.play();
                  setState(() { 
                    _isInitialized = true; 
                    _isLoading = false; 
                    _timeoutTimer?.cancel();
                  });
                }
                return true;
              } catch (e) {
                debugPrint("Video Error: $e");
                return false;
              }
            }

            void _onOptionPressed(String targetId) {
              if (targetId.isEmpty) return;
              setState(() { currentId = targetId; });
              _setupMedia();
            }

            @override
            Widget build(BuildContext context) {
              if (project == null) return const Scaffold(body: Center(child: CircularProgressIndicator()));
              final nodes = project!['nodes'] as Map<String, dynamic>;
              final node = nodes[currentId];
              if (node == null) return const Scaffold(body: Center(child: Text('FINISH')));

              return Scaffold(
                backgroundColor: Colors.black,
                body: Stack(
                  children: [
                    // 视频层
                    Positioned.fill(child: _buildMediaBackground(node)),
                    
                    // 遮罩层
                    Positioned.fill(
                      child: Container(
                        decoration: BoxDecoration(
                          gradient: LinearGradient(
                            begin: Alignment.topCenter,
                            end: Alignment.bottomCenter,
                            colors: [Colors.transparent, Colors.black.withOpacity(0.8)],
                          ),
                        ),
                      ),
                    ),

                    // 内容层
                    SafeArea(
                      child: Padding(
                        padding: const EdgeInsets.symmetric(horizontal: 40, vertical: 60),
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            const Spacer(),
                            if (_isLoading)
                              Padding(
                                padding: const EdgeInsets.only(bottom: 20),
                                child: Row(
                                  children: [
                                    const SizedBox(width: 20, height: 20, child: CircularProgressIndicator(strokeWidth: 2, color: Colors.cyanAccent)),
                                    const SizedBox(width: 15),
                                    Text(_loadingStatus, style: const TextStyle(color: Colors.cyanAccent, fontSize: 14)),
                                  ],
                                ),
                              ),
                            Text(node['title'] ?? '', style: const TextStyle(fontSize: 42, fontWeight: FontWeight.bold, color: Colors.white, letterSpacing: 1.2)),
                            const SizedBox(height: 20),
                            Container(
                              padding: const EdgeInsets.all(20),
                              decoration: BoxDecoration(
                                color: Colors.black.withOpacity(0.3),
                                borderRadius: BorderRadius.circular(12),
                                border: Border.all(color: Colors.white10),
                              ),
                              child: Text(node['content'] ?? '', style: const TextStyle(fontSize: 18, height: 1.8, color: Colors.white70)),
                            ),
                            const SizedBox(height: 40),
                            Wrap(
                              spacing: 15,
                              runSpacing: 15,
                              children: (node['options'] as List? ?? []).map((opt) => ElevatedButton(
                                style: ElevatedButton.styleFrom(
                                  backgroundColor: Colors.cyanAccent.withOpacity(0.1),
                                  foregroundColor: Colors.white,
                                  padding: const EdgeInsets.symmetric(horizontal: 30, vertical: 20),
                                  side: const BorderSide(color: Colors.cyanAccent),
                                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)),
                                ),
                                onPressed: () => _onOptionPressed(opt['targetId']?.toString() ?? ''),
                                child: Text(opt['label']?.toString() ?? '', style: const TextStyle(fontSize: 16)),
                              )).toList(),
                            ),
                          ],
                        ),
                      ),
                    ),

                    // 错误提示（非阻塞）
                    if (_errorMessage != null)
                      Positioned(
                        top: 20, left: 20, right: 20,
                        child: Container(
                          padding: const EdgeInsets.all(12),
                          decoration: BoxDecoration(color: Colors.amber.withOpacity(0.8), borderRadius: BorderRadius.circular(8)),
                          child: Row(
                            children: [
                              const Icon(Icons.warning_amber_rounded, color: Colors.black),
                              const SizedBox(width: 10),
                              Expanded(child: Text(_errorMessage!, style: const TextStyle(color: Colors.black, fontSize: 12, fontWeight: FontWeight.bold))),
                              IconButton(icon: const Icon(Icons.close, size: 16, color: Colors.black), onPressed: () => setState(() => _errorMessage = null))
                            ],
                          ),
                        ),
                      ),
                  ],
                ),
              );
            }

            Widget _buildMediaBackground(Map<String, dynamic> node) {
              if (_isInitialized && _videoController != null && _videoController!.value.isInitialized) {
                return SizedBox.expand(
                  child: FittedBox(
                    fit: BoxFit.cover,
                    child: SizedBox(
                      width: _videoController!.value.size.width,
                      height: _videoController!.value.size.height,
                      child: VideoPlayer(_videoController!),
                    ),
                  ),
                );
              }
              return Container(color: Colors.black);
            }
          }
          '@
          
          $dartCode.Trim() -replace '(?m)^\s{10}', '' | Out-File -FilePath "player_app/lib/main.dart" -Encoding utf8

      - name: 5. 注册资源与依赖
        shell: pwsh
        run: |
          cd player_app
          flutter pub add video_player
          $content = Get-Content pubspec.yaml
          $newContent = $content -replace '# assets:', "assets:`n    - assets/movie_config.json"
          Set-Content pubspec.yaml $newContent

      - name: 6. 编译 Release EXE
        run: |
          cd player_app
          flutter pub get
          flutter build windows --release

      - name: 7. 整理并打包产物
        shell: pwsh
        run: |
          if (Test-Path "output") { Remove-Item -Recurse -Force "output" }
          New-Item -ItemType Directory -Path "output"
          Copy-Item "player_app/build/windows/x64/runner/Release/*" -Destination "output/" -Recurse
          Compress-Archive -Path "output/*" -DestinationPath "Douju_Player_Windows.zip" -Force

      - name: 8. 上传 Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Douju-Flutter-EXE-Build
          path: Douju_Player_Windows.zip
          retention-days: 7
